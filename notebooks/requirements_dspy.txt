# DSPy Optimization Requirements
# Python >= 3.10 required

# Core dependencies
dspy>=3.0.3
mlflow>=3.4.0
python-dotenv>=1.0.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0

# Pydantic (DSPy compatibility)
pydantic>=2.0,<3.0

# Optional: for different LLM providers
# OpenAI
openai>=1.0.0

# Ollama (via DSPy's built-in support)
# No additional package needed, DSPy handles it

# ANEETAA dependencies (if not already installed)
langchain>=0.1.0
langchain-community>=0.0.1
langgraph>=0.0.1

# Development/Testing
datasets>=2.14.0
